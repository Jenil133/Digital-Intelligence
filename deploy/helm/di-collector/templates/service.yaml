apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: di-collector
spec:
  type: {{ .Values.service.type }}
  selector: { app: di-collector }
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      name: http
